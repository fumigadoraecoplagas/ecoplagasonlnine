<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperar Bodegas Eliminadas - Eco Plagas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="eco-plagas-styles.css?v=4.0" rel="stylesheet">
    <link href="unified-styles.css?v=1.0" rel="stylesheet">
    <script src="cache-buster.js"></script>
    <script src="check-permissions.js"></script>
    <script type="module" src="csrf-token.js"></script>
    <script type="module">
        import { auth } from './auth-secure.js';
        window.firebaseAuth = auth;
        (async function() {
            if (window.authCheckInProgress) return;
            window.authCheckInProgress = true;
            if (document.body) document.body.style.display = 'none';
            const hasSessionToken = sessionStorage.getItem('firebaseAuthToken') === 'authenticated';
            if (hasSessionToken && document.body) {
                document.body.style.display = 'block';
                document.body.classList.add('authenticated');
            }
            await new Promise(resolve => setTimeout(resolve, 500));
            let retries = 0;
            while (retries < 40) {
                if (auth.currentUser) {
                    if (window.secureAuthManager) window.authManager = window.secureAuthManager;
                    else {
                        await new Promise(resolve => setTimeout(resolve, 300));
                        if (window.secureAuthManager) window.authManager = window.secureAuthManager;
                    }
                    if (document.body && document.body.style.display === 'none') {
                        document.body.style.display = 'block';
                        document.body.classList.add('authenticated');
                    }
                    window.dispatchEvent(new CustomEvent('secure-auth-ready', { detail: { page: 'administrador_bodega_recuperar', timestamp: Date.now() } }));
                    window.authCheckInProgress = false;
                    return;
                }
                await new Promise(resolve => setTimeout(resolve, 100));
                retries++;
            }
            if (!hasSessionToken) window.location.replace('iniciar_sesion.html');
            window.authCheckInProgress = false;
        })();
    </script>
    <style>
        body { display: none !important; }
        body.authenticated { display: block !important; }
        
        .bodega-card {
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .bodega-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.15) !important;
        }
    </style>
</head>
<body>
    <div id="header-container"></div>
    <div class="container-fluid mt-2 px-3">
        <div class="page-header d-flex justify-content-between align-items-center">
            <h1><i class="fas fa-search inventory-icon"></i> Recuperar Bodegas Eliminadas</h1>
            <a href="administrador_bodega.html" class="btn btn-secondary"><i class="fas fa-arrow-left me-2"></i>Volver</a>
        </div>

        <div class="alert alert-info">
            <i class="fas fa-info-circle me-2"></i>
            <strong>Instrucciones:</strong> Esta herramienta busca bodegas que aparecen en transacciones pero que ya no existen en el sistema. 
            Puedes recrearlas con el mismo ID para que las transacciones vuelvan a ser rastreables en reportes y saldos.
        </div>

        <div class="section-content">
            <div class="section-card shadow-sm p-4">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h3 class="mb-0"><i class="fas fa-database"></i> Bodegas Desconocidas en Transacciones</h3>
                    <button class="btn btn-primary" onclick="buscarBodegasDesconocidas()">
                        <i class="fas fa-search me-2"></i>Buscar Bodegas Eliminadas
                    </button>
                </div>

                <div id="contenedorResultados" style="display: none;">
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        Se encontraron <span id="totalBodegasDesconocidas">0</span> bodega(s) desconocida(s) en las transacciones.
                    </div>

                    <div id="listaBodegasDesconocidas" class="row g-3 mt-3">
                        <!-- Se llenará dinámicamente -->
                    </div>
                </div>

                <div id="contenedorCargando" class="text-center py-5" style="display: none;">
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Cargando...</span>
                    </div>
                    <p class="mt-3 text-muted">Analizando transacciones...</p>
                </div>

                <div id="contenedorVacio" class="text-center py-5 text-muted">
                    <i class="fas fa-search fa-3x mb-3"></i>
                    <p>Haz clic en "Buscar Bodegas Eliminadas" para comenzar el análisis.</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal para crear bodega -->
    <div class="modal fade" id="modalCrearBodega" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title"><i class="fas fa-plus-circle me-2"></i>Recrear Bodega</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle me-2"></i>
                        Se creará la bodega con el ID: <strong id="modalBodegaId"></strong>
                    </div>
                    
                    <form id="formCrearBodega">
                        <input type="hidden" id="bodegaIdRecuperar">
                        
                        <div class="mb-3">
                            <label for="nombreBodegaRecuperar" class="form-label">Nombre de la Bodega *</label>
                            <input type="text" class="form-control" id="nombreBodegaRecuperar" required>
                            <small class="text-muted">Nombre sugerido basado en transacciones: <span id="nombreSugerido"></span></small>
                        </div>

                        <div class="mb-3">
                            <label for="tipoBodegaRecuperar" class="form-label">Tipo de Bodega *</label>
                            <select class="form-select" id="tipoBodegaRecuperar" required>
                                <option value="">Seleccionar tipo...</option>
                                <option value="principal">Principal</option>
                                <option value="rutero">Rutero</option>
                                <option value="especial">Especial</option>
                                <option value="apv">APV</option>
                            </select>
                        </div>

                        <div class="mb-3" id="contenedorEmpleado" style="display: none;">
                            <label for="empleadoBodegaRecuperar" class="form-label">Empleado</label>
                            <select class="form-select" id="empleadoBodegaRecuperar">
                                <option value="">Sin empleado asignado</option>
                            </select>
                        </div>

                        <div class="mb-3" id="contenedorVehiculo" style="display: none;">
                            <label for="vehiculoBodegaRecuperar" class="form-label">Vehículo</label>
                            <select class="form-select" id="vehiculoBodegaRecuperar">
                                <option value="">Sin vehículo asignado</option>
                            </select>
                        </div>

                        <div class="mb-3">
                            <label for="ubicacionBodegaRecuperar" class="form-label">Ubicación</label>
                            <input type="text" class="form-control" id="ubicacionBodegaRecuperar" placeholder="Opcional">
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-success" onclick="crearBodegaRecuperada()">
                        <i class="fas fa-check me-2"></i>Crear Bodega
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="utils.js"></script>
    <script type="module" src="load-headersecure.js"></script>
    <script type="module" src="administrador_bodega_recuperar.js"></script>
</body>
</html>



