# Configuración para evitar caché en desarrollo
<IfModule mod_headers.c>
    # Deshabilitar caché para archivos HTML
    <FilesMatch "\.(html)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>
    
    # Deshabilitar caché para archivos CSS y JS
    <FilesMatch "\.(css|js)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>
</IfModule>

# Configuración para Firebase Hosting
# Este archivo también funciona con Firebase Hosting


